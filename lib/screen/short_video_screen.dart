import 'package:flutter/material.dart';

class ShortVideoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('ShortVideo Screen'),
      ),
      body: Center(
        child: Text('ShortVideo Screen'),
      ),
    );
  }
}
